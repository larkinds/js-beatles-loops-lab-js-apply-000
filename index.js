// add solution here


const theBeatlesPlay = (arrayM, arrayI) => {
  let varE= []
  let emptyString = ''
  for (let i = 0; i < arrayM.length; i++) {
    emptyString = (arrayM[0] + ' plays ' + arrayI[0] + '.')
  }
}